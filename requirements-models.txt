# Optional lightweight open-source model providers for the prototype.
# Install this in addition to requirements.txt for faster-whisper ASR and OPUS-MT translation.
# Hindi TTS uses the system `espeak-ng` binary, not a Python model package.
faster-whisper>=1.1,<2.0
ctranslate2>=4.4,<5.0
numpy>=1.24,<2.0
torch>=2.2,<2.3
transformers>=4.46,<4.50
huggingface-hub>=0.24,<1.0
safetensors>=0.4,<1.0
sentencepiece>=0.2,<1.0
